pre-commit:
  commands:
    lint:
      run: pnpm lint
      stage_fixed: true
    format:
      run: pnpm format
      stage_fixed: true
    typecheck:
      run: pnpm typecheck

pre-push:
  commands:
    test:
      run: pnpm test:run
    build:
      run: pnpm build

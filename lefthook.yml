pre-commit:
  commands:
    lint:
      run: pnpm lint
      stage_fixed: true
    format:
      run: pnpm format
      stage_fixed: true
    typecheck:
      run: pnpm typecheck

pre-push:
  commands:
    build:
      run: pnpm build
